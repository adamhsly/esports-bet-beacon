
// Re-export the toast functionality from the UI component
import { useToast, toast, type ToastActionElement, type ToastProps } from '@/components/ui/use-toast';

// Export everything from the use-toast module
export { useToast, toast, type ToastActionElement, type ToastProps };
